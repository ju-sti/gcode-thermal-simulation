class Point {

    constructor(x, y) {
        this.x = x;
        this.y = y;
    } // end constructor

    getX() {
        return this.x;
    }

    getY() {
        return this.y;
    }

    // toString() {
    //   return "(" + this.x + ", " + this.y + ")";
    // }
} // end Point
